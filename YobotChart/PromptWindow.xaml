<Window
    x:Class="YobotChart.PromptWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="PromptWindow"
    Width="437.805"
    Height="271.463"
    ResizeMode="NoResize"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Content="登录链接（私聊Bot“登录”）：" />
        <TextBox
            x:Name="TextBox"
            Grid.Row="1"
            Margin="5,5,5,0"
            AcceptsReturn="True" />
        <Button
            Grid.Row="2"
            Margin="5"
            Padding="18,2"
            HorizontalAlignment="Center"
            Click="Button_Click"
            Content="确定" />
    </Grid>
</Window>
